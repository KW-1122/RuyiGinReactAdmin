# Ruyi Gin React Admin

前后端分离的纯 AI 驱动现代管理系统。

## 目录

- `backend`：后端代码目录，后续接入 Gin。
- `frontend`：前端代码目录，当前使用 React + Vite，接口先采用 mock。

## 布局偏好

默认后台管理布局采用：顶部固定高度、左侧固定宽度、内容区自适应；页面整体宽高等于屏幕宽高，内容区超出后独立滚动。

## 前端启动

```bash
cd frontend
npm install
npm run dev
```
